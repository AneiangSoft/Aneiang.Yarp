---
description: 项目开发规范
alwaysApply: true
---

# 开发规范

## 通用规范

1. **语言**：永远使用中文回复
2. **编译验证**：每次代码改动后必须进行编译，确保代码能正常运行
3. **Bug 修复**：修复 Bug 时需要：
   - 检查相关逻辑是否需要一起修改
   - 检查其他模块是否有同样的问题
4. **单一职责**：函数和类只做一件事，保持职责单一
5. **图标规范**：前端禁止使用表情图标 (emoji)，统一使用字体图标 (Font Icon)
6. **未实现逻辑**：如有未实现的逻辑，需要明确列出说明

## 代码质量

- 避免冗余注释，注释只解释非显而易见的意图或约束
- 代码修改后检查是否有 lint 错误并修复
